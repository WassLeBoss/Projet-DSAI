# Features stylométriques (pairwise)
# Utilisé uniquement avec dataset=wac (mode pairwise)
name: features
_target_: src.encoders.features_encoder.FeaturesEncoder

scale: true          # StandardScaler sur les features
random_state: 42     # Graine pour la symétrie pairwise
